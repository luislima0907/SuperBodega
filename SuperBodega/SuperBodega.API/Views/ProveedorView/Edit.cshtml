@{
    ViewData["Title"] = "Editar Proveedor";
    Layout = "_DashboardLayout";
}

@section SidebarMenu {
    <partial name="_SidebarMenuPartial" />
}

<div class="container-fluid">
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0">Edición de Proveedores</h1>
        <a href="/Proveedores/Index" id="btnVolver" class="btn btn-outline-primary me-2">
            <i class="fas fa-arrow-left me-1"></i> Volver al Listado
        </a>
    </div>

    <div id="alertContainer" class="mb-4" style="display: none;"></div>

    <div class="row">
        <div class="col-lg-8">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold">Editar Proveedor</h6>
                </div>
                <div class="card-body">
                    <form id="formEditProveedor">
                        <input type="hidden" id="idProveedor" name="idProveedor">
                        <div class="mb-3">
                            <label for="nombre" class="form-label">Nombre</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="bi bi-truck"></i></span>
                                <input type="text" class="form-control" id="nombre" name="nombre" placeholder="Nombre de la empresa o proveedor">
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="email" class="form-label">Email</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="bi bi-envelope"></i></span>
                                <input type="email" class="form-control" id="email" name="email" placeholder="contacto@proveedor.com">
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="telefono" class="form-label">Teléfono</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="bi bi-telephone"></i></span>
                                <input type="tel" class="form-control" id="telefono" name="telefono" placeholder="Ej: 2222-2222">
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="direccion" class="form-label">Dirección</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="bi bi-geo-alt"></i></span>
                                <textarea class="form-control" id="direccion" name="direccion" rows="2" placeholder="Dirección completa del proveedor"></textarea>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="estado" class="form-label">Estado</label>
                            <div class="form-check form-switch">
                                <input type="checkbox" class="form-check-input" id="estado" name="estado" checked>
                                <label class="form-check-label" for="estado">Activo</label>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="fechaDeRegistro" class="form-label">Fecha de Registro</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="bi bi-calendar-date"></i></span>
                                <input type="hidden" id="fechaDeRegistro" name="fechaDeRegistro">
                                <input type="text" class="form-control" id="fechaDeRegistroVisible" readonly>
                            </div>
                            <small class="text-muted">La fecha de registro no se puede modificar.</small>
                        </div>
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a href="@Url.Action("Index", "ProveedorView")" class="btn btn-secondary me-md-2">
                                <i class="fas fa-ban me-1"></i>Cancelar
                            </a>
                            <button type="button" id="btnRestaurarProveedor" class="btn btn-warning me-2">
                                <i class="fas fa-rotate-left me-1"></i>Restaurar
                            </button>                            
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-sync-alt me-1"></i>Actualizar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <div class="col-lg-4">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold">Información Adicional</h6>
                </div>
                <div class="card-body">
                    <p>Al actualizar un proveedor, se reflejarán los cambios en todos los productos y compras asociados.</p>
                    <hr>
                    <h6 class="font-weight-bold">Consideraciones:</h6>
                    <ul>
                        <li>Desactivar un proveedor ocultará todos sus productos y compras en las búsquedas.</li>
                        <li>No se recomienda cambiar el nombre si ya tiene productos y compras asociados.</li>
                        <li>La fecha de registro no puede ser modificada.</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/proveedores.js"></script>
}