@{
    ViewData["Title"] = "Editar Categoría";
    Layout = "_DashboardLayout";
}

@section SidebarMenu {
    <partial name="_SidebarMenuPartial" />
}

<div class="container-fluid">
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0">Editar Categoría</h1>
        <a href="#" id="btnVolver" class="btn btn-outline-primary me-2">
            <i class="fas fa-arrow-left me-1"></i> Volver al Listado
        </a>
    </div>

    <div id="alertContainer" class="mb-4" style="display: none;"></div>

    <div class="row">
        <div class="col-lg-8">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold">Información de la Categoría</h6>
                </div>
                <div class="card-body">
                    <form id="editCategoriaForm">
                        <input type="hidden" id="categoriaId" name="id">
                        <div class="mb-3">
                            <label for="nombre" class="form-label">Nombre <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="bi bi-tag-fill"></i></span>
                                <input type="text" class="form-control" id="nombre" name="nombre" placeholder="Nombre de la categoría">
                            </div>
                            <div class="invalid-feedback">El nombre es obligatorio.</div>
                        </div>
                        <div class="mb-3">
                            <label for="descripcion" class="form-label">Descripción</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="bi bi-textarea-t"></i></span>
                                <textarea class="form-control" id="descripcion" name="descripcion" rows="3" placeholder="Descripción detallada de la categoría"></textarea>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="estado" class="form-label">Estado</label>
                            <div class="form-check form-switch">
                                <input type="checkbox" class="form-check-input" id="estado" name="estado">
                                <label class="form-check-label" for="estado">Activo</label>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label for="fechaDeRegistro" class="form-label">Fecha de Registro</label>
                            <div class="input-group">
                                <span class="input-group-text"><i class="bi bi-calendar-date"></i></span>
                                <input type="hidden" id="fechaDeRegistro" name="fechaDeRegistro">
                                <input type="text" class="form-control" id="fechaDeRegistroVisible" readonly>
                            </div>
                            <small class="text-muted">La fecha de registro no se puede modificar.</small>
                        </div>
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <a href="@Url.Action("Index", "CategoriaView")" class="btn btn-secondary me-md-2">
                                <i class="fas fa-ban me-1"></i>Cancelar
                            </a>
                            <button type="button" id="btnRestaurarCategoria" class="btn btn-warning me-2">
                                <i class="fas fa-rotate-left me-1"></i>Restaurar
                            </button>                            
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-sync-alt me-1"></i>Actualizar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
        <div class="col-lg-4">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold">Información Adicional</h6>
                </div>
                <div class="card-body">
                    <p>Al actualizar una categoría, se reflejarán los cambios en todos los productos asociados.</p>
                    <hr>
                    <h6 class="font-weight-bold">Consideraciones:</h6>
                    <ul>
                        <li>Desactivar una categoría ocultará todos sus productos en las búsquedas.</li>
                        <li>No se recomienda cambiar el nombre si ya tiene productos asociados.</li>
                        <li>La fecha de registro no puede ser modificada.</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/categorias.js"></script>
}